import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#1a237e',
      light: '#534bae',
    },
    secondary: {
      main: '#ff3d00',
    },
    background: {
      default: '#0a1929',
      paper: 'rgba(255, 255, 255, 0.95)',
    },
  },
});
